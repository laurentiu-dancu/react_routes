<?php

/**
 * @file
 * Hook implementations for react_routes.
 */

/**
 * Implements hook_theme().
 */
function react_routes_theme($existing, $type, $theme, $path) {
  return [
    'react_render' => [
      'variables' => [
        'component' => 'App',
        'props' => [],
      ],
    ],
  ];
}
